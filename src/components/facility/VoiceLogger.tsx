// Placeholder — Week 4: Hindi voice stock logging component

interface VoiceLoggerProps {
  facilityId: string | null
  onApplied: () => void | Promise<void>
}

export default function VoiceLogger(_props: VoiceLoggerProps) {
  return null
}